apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer
  namespace: kafka-clients
  labels:
    app: producer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: producer
  template:
    metadata:
      labels:
        app: producer
    spec:
      containers:
        - name: producer
          image: python:3.12-slim
          command: ["sh", "-c"]
          args:
            - pip install --no-cache-dir --quiet confluent-kafka==2.6.1 && exec python /app/producer.py
          env:
            - name: PRIMARY_BOOTSTRAP_SERVERS
              value: primary-cluster-kafka-bootstrap.kafka-primary.svc:9092
            - name: DR_BOOTSTRAP_SERVERS
              value: dr-cluster-kafka-bootstrap.kafka-dr.svc:9092
            - name: TOPIC
              value: drc-demo-topic
          volumeMounts:
            - name: app-code
              mountPath: /app
              readOnly: true
            - name: active-cluster
              mountPath: /etc/active-cluster
              readOnly: true
          resources:
            requests:
              cpu: "50m"
              memory: 128Mi
            limits:
              cpu: "200m"
              memory: 256Mi
      volumes:
        - name: app-code
          configMap:
            name: client-app-code
        - name: active-cluster
          configMap:
            name: active-cluster-config
